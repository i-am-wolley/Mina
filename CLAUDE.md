# Mina — session memory

Mina is a document-first, AI-assisted household wealth intelligence platform (net worth, investments, taxes, spending, reasoning-over-reporting). Full product spec: `Base resources/investment_app_memo.md` (1196 lines — read it, don't guess from this summary). **This file is build-state truth; the memo is product truth.** When they conflict on a build detail, this file wins (it reflects decisions made after the memo was written).

Build plan for the current phase: `C:\Users\Vinod\.claude\plans\tidy-floating-leaf.md`.

## Locked architecture decisions

| Decision | Why |
|---|---|
| **Miso Design System is MANDATORY for all UI work, effective 2026-08-02** — no more invented palettes | User's explicit instruction. The real system was found at `https://claude.ai/design/p/7901307e-8140-4a72-82b4-3654d76d0886` (GitHub: `i-am-wolley/Miso`) and is installed locally at `~/.claude/skills/miso-design/` (global — applies beyond this repo). See "Design tokens" below for what changed. |
| **Stack: single `index.html`, no build step** — Preact + `htm`, pinned ESM CDN imports (never `@latest`) | Kept from memo §17.2 in case this later merges into a real Miso repo. No bundler, no TypeScript, no JSX transpile (S1). |
| **UI-first, mock data before backend** | User wants visual progress before infra. Mock fixtures are written to match the memo's §3.2 canonical shapes exactly, so wiring real data later is a data-source swap, not a rewrite. |
| **Firebase/backend deferred to Stage 8** | Not started. No Firebase project exists yet. Don't assume one when reading this file in a future session. |
| **"Landing page" = the TODAY tab** | Not a separate marketing page — memo §14B.3/§14B.6 already makes Today's empty state the onboarding invitation. |
| **Desktop-only for now, mobile deferred — effective 2026-08-02** | User's explicit instruction: "why are u building for mobile... build only for web for now." The memo's mobile-first shell (§17.2, `app-max-w: 440px`, bottom `TabBar`) is **replaced** by wide multi-column content. The 390px/mobile verification steps in the original plan no longer apply until mobile is explicitly revisited (memo §17.2.7's wide-layout pass, inverted — now mobile is the deferred pass, not desktop). |
| **Nav shell is a TOP bar, not a left sidebar — effective 2026-08-03** | User's explicit instruction: "first in all pages move the sidebar to the top.. we get more estate." The `Sidebar` component (left column, `--sidebar-w`) was fully removed and replaced by `TopNav` (`.topnav`, sticky top row) so every screen gets full-width content instead of losing ~230px to a left rail. `--content-max-w` widened to 1560px to use the reclaimed space. Applies globally (`AppShell` renders `TopNav` once, not per-screen). |

## AI boundaries (memo §16 — P1)

**AI may:** classify/extract document fields (with mandatory arithmetic reconciliation), normalise merchant names, summarise news, resolve ambiguous instrument names, turn a computed flag into prose, translate NL queries into structured queries, explain a concept on demand.

**AI must never:** compute a return/tax/valuation/projection, decide a classification that affects tax treatment (propose only — a deterministic rule confirms), generate a UI number with no code path behind it, or recommend a buy/sell on a specific instrument.

This matters now even in mock-data UI stages: **no chart or card should compute a derived number in the render layer** — mock fixtures should already carry the computed value (e.g. XIRR), never have the UI calculate it from raw transactions. Keep that discipline from day one so it isn't a retrofit at Stage 8.

## File map — `index.html`

Section-banner order per memo §17.2. Update the ✅ column as sections are actually added — don't let this list drift from the file.

| # | Section | Status |
|---|---|---|
| 01 | `<head>` · meta · fonts · design tokens (CSS vars) | ✅ |
| 02 | Global styles · component primitives · motion | ✅ |
| 03 | ESM imports (pinned) | ✅ |
| 04 | Firebase init · Auth · App Check | not started (Stage 8) |
| 05 | Data layer — Firestore reads, offline cache, household scope | not started (Stage 8) |
| 06 | Content-pack loader (taxonomy, rules, copy, tax packs) | not started |
| 07 | Formatters — currency, %, dates, locale, jargon glossary | ✅ |
| 08 | Shared UI — Sheet (not yet), Card, Badge, Sparkline, **Donut** (new), EmptyState, Skeleton | ✅ (Sheet still missing — needed Stage 5) |
| 09 | Chart wrappers (ECharts, lazy) | **still not started** — the Portfolio donut is a hand-rolled SVG (`Donut` component), deliberately not ECharts, to avoid the dependency for one ring. Sunburst/treemap/correlation-heatmap will need real ECharts when built. |
| 10 | Screen: Today | ✅ desktop v2, **"Needs You" replaced 2026-09-03** — two-column (`today-grid`: hero+cards+mini-allocation left, agenda+Immediate/Future actions right). Agenda widened **"Next 7 days" → "Next 14 days" 2026-09-05**; the review-queue "documents not yet processed" item was dropped from Immediate Actions the same day (not actionable, user asked it not read as a to-do on Today). See "Today: Immediate/Future actions" section below and "14-day agenda + real earnings/dividend calendar" further down. |
| 11 | Screen: Portfolio (+ Position detail, Analysis) | ✅ **v8, 2026-09-04** — v7 (dual hero cards → By-asset-class bar row → class-button filter bar + Member dropdown → full-width sortable table, default **All**) plus an **Invested** column (real `cost_basis`, "—" where unknown — same honesty rule as Avg Cost) sitting next to Avg Cost, with group-header rows now also summing a real per-group invested total. See "Portfolio: Invested column, real invested-to-date feeds the Goal chart" section below. Position-detail as a separate drill-down screen still not built — row selection + the Insights tab cover most of that need already. Shows a second AB InBev RSU row (`pos_v_rsu_2`, a 191-unit vest reported 2026-09-04) alongside the original 255-unit lot — same multi-lot-per-instrument pattern already used for Keerthana's two Parag Parikh Flexicap folios. Foreign Equity gained a 16th real holding, **Vanguard Total International Stock ETF (VXUS)**, on 2026-09-05 (Vinod's Alpaca account) — see "A genuinely new holding: VXUS" section below. **2026-09-06**: split a wrongly-merged ICICI Next 50 purchase into its own Direct Plan row (12th MF), corrected a ~₹1L pre-existing data error on Axis Small Cap, and added 2 more Edelweiss Liquid Fund lots — see "Nifty 50 Direct-plan split, an Axis Small Cap correction, 2 more Edelweiss lots" below. |
| 12 | Screen: Insights (+ Flag decision, Assumptions) | ✅ **built 2026-08-03** — see "Insights tab" section below for the full design. Class-button + Member filter (same pattern as Portfolio) drive a full-width, data-driven grid of sections (trend/growth, insights, allocation + cross-class comparison at the household level, sector exposure, technical range scan, leaders & laggards, concentration scan, FD maturity ladder, market news) — Mutual Funds keeps its own deeper 7-section panel. Flag-decision / Assumptions sub-screens (memo §11.2/§10) still not built. |
| 13 | Screen: Money (+ Calendar, Spending, Tax) | ✅ built, **hidden from nav 2026-09-02** (explicit user request — screen/route/data untouched, just not in `NAV_ITEMS`). See "Money tab v2" section below (and v1 below that). Real HDFC Infinia data at two layers: statement summaries (6, Feb–Jul 2026 only) drive spend trend/utilisation/revolving-balance/statement history; 514 real individual transactions (`credit-card-transactions.js`, new this pass) drive a real category mix + a searchable transaction list with a working reclassify popup. Typical-month baseline replaces a misleading flat average. Income/savings-rate/runway still honestly gap-noted (no bank statement on file). No calendar sub-screen or real tax computation yet. |
| 13a | Screen: Goal — ₹20 Cr growth plan | ✅ **built 2026-09-02, projection chart replaced 2026-09-03, chart re-scaled + allocation donut replaced 2026-09-04, Today's bar corrected to a real invested-vs-grown split 2026-09-04** — see "Goal tab" section below. A 10-year forward-looking scenario calculator (optimistic/base/pessimistic), not a historical report — deliberately computes live from disclosed constants (`GOAL_PLAN`), which is a different discipline than the "never compute a return in the render layer" rule elsewhere (that rule is about historical actuals, not a what-if tool). The old all-3-scenarios line chart was replaced with a single-scenario **year-over-year stacked bar chart** (invested vs. growth, real values labeled, scenario-toggle buttons) — see "Goal tab: YoY invested-vs-growth bars" section below. **Real estate was briefly added as its own bucket, then explicitly reverted the same day** — this goal tracks investable/liquid wealth only; immovable property is a completely separate concept, not a bucket in this model at all (see Debt & Immovable Assets). Also carries a **purchasing-power reality check** (₹20 Cr in 2036 ≈ ₹12.28 Cr today's money at base-case inflation) and an **"FD instead" comparison** (post-tax, then inflation-adjusted) — see "Purchasing-power reality check" section below. **Drifted from the standalone `investment-plan-20cr.html`**, which has neither section — a known, disclosed gap, not fixed the same day. |
| 13b | Screen: Budget — real income/outflow breakdown | ✅ **built 2026-09-02, corrected twice same day** — see "Budget tab" section below. First real income figure Mina has ever had (`mock-data/budget.js`), first real computed savings rate (**46.25%**, MF SIP only — the initial ~48% figure double-counted 2 insurance-premium sinking funds as investment, corrected). One representative month, not a trend yet. Carries the real liquidity-runway figure (Edelweiss Liquid Fund vs. essential spend) and a live "budget health" insights section (savings-rate/EMI-ratio benchmarks, largest discretionary lever, family-support total, essential-spend leanness). |
| 13c | Screen: Debt & Immovable Assets — loans, real estate, debt-vs-invest | ✅ **built 2026-09-02, restructured same day** — see "Debt tab" section below. 3 real loan accounts (`mock-data/liabilities.js`, account numbers masked to last 4 digits) against 2 real properties (`mock-data/real-estate.js` — deliberately its own file, NOT `positions.js`/`instruments.js`, so it can never leak into any screen's net-worth sum). Compares each loan's real rate against the Goal plan's own new-SIP scenario rates — the math, not a recommendation. The one place these 2 properties are shown; excluded from `householdTotals.current_total` and the Goal plan by explicit request. |
| 14 | Sheet: Add / Upload / Review queue (pdf.js, lazy) | not started (Stage 5) |
| 15 | Settings | not started (Stage 6) — top nav gear icon exists, screen doesn't |
| 16 | Router · **top nav** shell (replaced sidebar 2026-08-03, which had replaced tab-bar+header) · household switcher | ✅ desktop shell (`TopNav` + `.content`); switcher is visual-only, doesn't filter data yet |
| 17 | Mount | ✅ |

## Design tokens — Miso Design System (mandatory)

**Superseded 2026-08-02.** The dark neutral fintech palette this file originally described is gone. Every screen must use the real **Miso Design System** — tokens, component shapes, and iconography pulled directly from `https://claude.ai/design/p/7901307e-8140-4a72-82b4-3654d76d0886` (source app: `github.com/i-am-wolley/Miso`, Vinod's own sibling "Pantry OS" app). A working local mirror (tokens + every component `.jsx`) lives at `~/.claude/skills/miso-design/` — **read that skill's `readme.md` before touching any UI**, it's the actual spec, not this summary.

**What's actually in `index.html` section 01 now** — Miso's real values, not paraphrased:
- Warm paper surface (`--cream-100 #f5f0ea`), warm near-black ink text (`--ink-900 #18140f`) — light theme is canonical; a dark-theme override exists but is secondary (Miso itself is light-first, inverted from what this file said before).
- Interactive accent is **bronze** (`--bronze-500 #b8935a`), not the invented blue accent.
- Severity/gain-loss now ride Miso's actual category-accent set, not invented hex values: gain = `--sage-500`, loss = `--danger-500`, flag severity Critical/High/Medium/Watch = danger/terracotta/amber/lilac respectively. Same discipline as before (one ramp, reused everywhere, colour never the sole signal) — just Miso's real hues instead of made-up ones.
- Type is **Geist** + **Geist Mono** (loaded from Google Fonts per Miso's own `fonts.css` substitution note — swap to licensed binaries if the user ever provides them), not the old system-font stack.
- Spacing/radii/shadows/motion values are copied verbatim from `tokens/spacing.css` (4px-based scale, `--radius-xl` cards, barely-there `--shadow-card`, 0.12s/0.3s/0.7s durations) — not re-derived.
- Cards, badges, the tab bar, and the hero number now follow Miso's actual Card/Badge/StatCard/TabBar shapes (hairline border, soft radius, flat warm fill) rather than a fintech dark-card look.

**Iconography — no more emoji, ever.** Mina's `Icon` component (section 08a of `index.html`) uses Miso's real 24×24 hand-drawn stroke technique (`stroke="currentColor"`, `strokeWidth="1.8"`, round caps/joins, no fill except tiny accent dots). Miso's 38 glyphs are food-domain and mostly don't apply here; where Mina needs something Miso doesn't have (`home`, `chart`, `bulb`, `wallet`, `flag`, `newspaper`, `inbox`, `gear`, `plus`, `chevronDown`), new glyphs were hand-drawn in the *identical* technique — never a mismatched icon font, never emoji. `calendar` deliberately aliases Miso's own `menuTab` glyph (it's already a calendar shape). **When a future screen needs an icon not yet in `index.html`'s `Icon` function, check the real 38-glyph list first (`~/.claude/skills/miso-design/readme.md` → Iconography), reuse a generic one if it fits (`sparkle`, `receipt`, `link`, `upload`, `camera`, `image`, `refresh`, `bookmark`), otherwise draw a new one in the same technique. Do not fall back to emoji.**

- **When building any chart, load the `dataviz` skill first** for the categorical/sequential palette logic, but anchor its output hues to Miso's actual accent set (sage/amber/terracotta/lilac/teal + bronze) rather than a generic placeholder palette.

Not frozen — expect refinement (e.g. wiring the actual `_ds_bundle.js` mount pattern) once more screens exist to review against.

## Canonical data shapes (matches memo §3.2)

Fixtures live in `mock-data/`. Each mirrors the real entity shape from the memo so Stage 8 is a swap, not a rewrite:

```
Household ──< Member ──< Account ──< Position
                            │
                            └──< Transaction ──> Instrument
```

Transaction type vocabulary is closed per memo §3.2 (BUY, SELL, DIVIDEND, …) — don't invent new transaction types in fixtures.

### These fixtures now hold REAL household data, not invented numbers

Mid-build, the user supplied real statements/screenshots (`Base resources/Statements to look and build/`) and asked for them to replace the placeholder Vinod/Keerthana portfolio. Every position in `mock-data/positions.js` and `mock-data/instruments.js` is now sourced from an actual document or a live INDmoney pull, with per-instrument `source` provenance. This is **not** the real backend (still no Firestore, no auth, no ingestion pipeline) — it's real *numbers* sitting in the same static-fixture architecture as before. Treat these files as sensitive when editing: no PAN/UAN/account numbers/addresses were carried in, and that should stay true for any future edits.

**Sources used:**
- `Vinod MF.pdf`, `Keerthana MF.pdf` — CAMS/KFintech consolidated account statements (mutual funds, real folios/ISINs/units/NAV)
- `Vinod_india Stocks.png` — Zerodha holdings screenshot (invested amounts); current values refreshed live via the `claude_ai_Indmoney_Vinod` MCP connector (Vinod's account only — see below)
- `Vinod US Stocks.xls`, `Keerthana us stocks.xls` — Alpaca/DriveWealth order books; net holdings computed from BUY/SELL rows. Vinod's (Alpaca) got live INR values via INDmoney; Keerthana's (DriveWealth) did not — see gaps below
- `FD With invested and current value _Vinod.png`, `Vinod_RSU.png`, `Keerthana_Gold.png` — screenshots, used as-is
- `Vinod PF Yearwise investment statement/*.pdf` (2017–2026) — EPFO passbooks; full year-end history captured in `mock-data/epf-history.js`, current balance used in `positions.js`
- 7 months of HDFC Infinia credit card statements (Jan–Jul 2026) — summary-only in `mock-data/credit-card-statements.js` (no line items; Money tab territory, Stage 4)
- The `claude_ai_Indmoney_Vinod` MCP connector — used once for `networth_holdings` (MF/IND_STOCK/US_STOCK/FD/EPF, Vinod's account only). **Do not call `networth_snapshot` or other Indmoney MCP tools without the user's explicit go-ahead** — they asked to stop using that connector mid-session.

**Known gaps, deliberately left as `null`/flagged rather than guessed (P8):**
- Keerthana's 9 DriveWealth US-stock positions are now priced — via a one-time web-search price lookup + spot USD/INR (₹95.40) done on 2026-08-02, recorded in `positions.js` as `PRICE_ASOF`. This is a manual mark, **not** a wired live feed (that's still Stage 8), so don't treat these 9 values as continuously current the way the INDmoney-sourced ones are.
- No cash/savings account statement for either member — household liquidity is currently invisible to net worth.
- `householdTotals.delta_today` / `delta_month_pct` / `xirr` / `twr` are still **synthetic placeholders** — real daily deltas need the nightly snapshot pipeline (§15, Stage 8) that doesn't exist yet. The `current_total` and every individual position value are real.
- `netWorthHistory`'s 90-day sparkline is still a synthetic random walk (see its file header) — only anchored to end at the real `current_total`, not real daily history.
- The STRUCT-02 flag in `flags.js` is an illustrative scenario layered onto a real holding (Axis ELSS Tax Saver) — no manager-exit is actually confirmed. Said so in the file.
- ~~Keerthana's US-stock `cost_basis` is stored as `cost_basis_usd`, not INR — so Portfolio's gain% column currently shows `—`~~ **Fixed 2026-08-03**: `gainPctOf()` and `impliedCostBasis()` in `index.html` now check `cost_basis_usd * PRICE_ASOF.usd_inr` whenever `cost_basis` is null, so all 9 DriveWealth positions show a real gain% and feed correctly into peer-comparison/aggregate-return insights.

**2026-08-02, later same day — data freshness pass, then a correction.** The user asked to verify every asset was priced *today*. Vinod's MF/stocks/US-stocks were already live via INDmoney, no change needed. Keerthana's 9 mutual funds were sitting on the 29-Jun CAS NAV, so they were repriced via web-search NAV lookups — **this was a mistake.** Different providers returned different, undated NAVs for the same fund, and one fund (HDFC Sensex Regular) had no real source at all — I estimated it from a proxy calculation and presented it as today's value anyway. The user caught this ("values not accurate") and it was reverted: **all 9 of Keerthana's MF positions are back on the 29-Jun-2026 CAS statement NAV**, clearly labeled as such rather than dressed up as current. Gold ETF and Vinod's RSU kept their web-search updates — those came from single, unambiguous quotes, not the fragmented multi-provider mess the MF lookups hit. `current_total`: ₹4,02,75,896 (original) → ₹4,05,15,753 (bad reprice) → **₹4,03,33,852 (current, corrected)**.

**2026-08-02, same day, third pass — the actual fix.** The user pushed back again ("navs are missing and quantity as well, also the navs are incorrect across") and asked to recheck everything. This turned up the real fix: **AMFI publishes an official daily NAV file** at `portal.amfiindia.com/spages/NAVAll.txt` (~17,700 lines, all schemes, by exact ISIN) — a real regulator-mandated source, not a scraped aggregator. Downloaded it directly and grepped for all 11 held ISINs (Vinod's 2 funds + Keerthana's 9). Two findings: (1) Keerthana's 9 funds got real, dated, corroborated NAVs this time (mostly 31-Jul-2026, one 02-Aug-2026) — no more guessing. (2) **Vinod's 2 funds, which I'd assumed were fine because they came "live via INDmoney," were actually ~0.7% off** the AMFI-declared NAV for the same ISIN — INDmoney's live pull was not as authoritative as the official source. All 11 MF positions now use AMFI data uniformly. `current_total`: → **₹4,05,99,381**. Every position now also carries a `nav` field (see below) for on-screen display.

**Standing lesson — apply this every time NAV/price accuracy comes up:** (1) "Live via a connected account" is not automatically more authoritative than a regulator-published source — check both when you can, don't assume the fancier-sounding one wins. (2) For Indian MF NAVs specifically, **use AMFI's NAVAll.txt by ISIN** (`curl`/`WebFetch` + grep — WebFetch's own summarizer chokes on the file's size and misses schemes, so download it and grep directly). (3) Generic web search across scraper sites remains unreliable for anything price-dated — don't fall back to it for NAVs again. (4) Stocks/RSU/gold still only have single-source web quotes, not independently cross-checked the way the MF NAVs now are — flagged as a smaller residual risk of the same kind, not yet re-verified.

**2026-08-02, fourth pass — stocks and RSU got the same treatment.** User pushed once more ("indian and us stocks also qnt and nav — check it properly"). Found a real, structured, single-authority source for these too: **Yahoo Finance's `query1.finance.yahoo.com/v8/finance/chart/<TICKER>` endpoint** — no key needed, works for both NSE tickers (`.NS` suffix) and US tickers directly, returns `regularMarketPrice` cleanly. Verified all 10 Indian stocks (Solex Energy only resolved as `SOLEX.NS`, not the guessed `SOLEXENERGY.NS`), all 13 US positions (4 Vinod + 9 Keerthana), and the RSU ticker (BUD). Findings: Vinod's stocks/US-stocks (from INDmoney) were already accurate to within ~2%, no real error — but Keerthana's MU (-1.3%) and **ASML (-9.6%, real price $1629 vs the web-search figure of $1801.51)** were genuinely wrong, and RSU/BUD was off **5.9%** ($81.66 vs real $86.48). `current_total`: → **₹4,07,01,024**. Every stock/RSU position now has a `nav_asof` citing Yahoo Finance.

**Updated standing lesson:** the fix for "I don't trust this price" is the same pattern every time — find the *structured, single-authority* API (AMFI for Indian MF NAVs, Yahoo Finance's chart endpoint for stocks/ADRs, both fetched via `curl`/`WebFetch` + parsed directly), never generic web search, which reliably produces multiple disagreeing numbers with no reliable date. If a future asset class needs verification and neither AMFI nor Yahoo covers it, look for that asset class's own equivalent regulator/exchange source before falling back to search.

**2026-09-02 — second full reprice pass, exact same method, one month later.** The user flagged that Portfolio was showing stale NAV/stock values (the 2026-08-02 pull was a month old by then). Re-ran the identical two-source method — no new technique needed, which is itself a good sign the method from the passes above was sound: AMFI's `NAVAll.txt` (curl'd directly, grepped by the same 10 ISINs, all dated 01-Sep-2026) for the 11 MF positions, Yahoo Finance's chart API (curl'd directly with a browser `User-Agent` header — a bare `curl` got HTTP 429 from Yahoo, adding the header fixed it) for all 20 stock/RSU positions plus the live USD/INR rate (94.908, was 95.39). Confirmed **`curl` has real outbound internet access from the Bash tool in this environment** (not just `WebFetch`/`WebSearch`) — useful to remember next time a large structured file needs grepping rather than summarizing. Gold was left at its prior mark (no fresh single-source quote pulled this pass, disclosed in `PRICE_ASOF`). `current_total`: ₹4,07,01,024 → **₹4,09,90,401** (+₹2,89,377, +0.71% over the month) — modest, broad-based, no single position drove it. `delta_month_pct` in `householdTotals` is now a **real** computed figure (the actual change between the two reprice dates), not the synthetic placeholder it was before — `delta_today` still is, since that still needs the nightly snapshot pipeline. Both `investment-plan-20cr.html` and the in-app Goal tab picked up the new total automatically (Goal tab reads `householdTotals.current_total` live; the standalone sheet needed its hardcoded constant updated by hand, since it has no shared import with `mock-data/`).

## Nifty 50 Direct-plan split, an Axis Small Cap correction, 2 more Edelweiss lots (2026-09-06)

Three data-accuracy fixes in one message, all against the Keerthana MF block.

**ICICI Next 50 — the 2026-09-05 purchase was on the wrong plan.** That day's "ICICI Prudential Nifty Next 50 Index Fund" purchase had been appended to `pos_k_mf_6`, the household's existing **Regular Plan** holding (ISIN `INF109K01IF1`). The user clarified the actual purchase was the **Direct Plan** — a genuinely different fund with its own ISIN (`INF109K01Y80`, verified via AMFI's NAVAll.txt) and NAV (67.8462 vs. the Regular Plan's 64.1055), not a top-up of the existing lot. Reverted `pos_k_mf_6` to its pre-2026-09-05 units/cost_basis (1,971.132 units / ₹60,000), and moved the +869.57 units / +₹55,744.22 onto a new position (`pos_k_mf_10`) against a new instrument (`inst_icici_next50_direct`). **Lesson for future purchases**: "same fund name" isn't enough to confirm it's the same holding — Direct vs. Regular (and different AMCs' near-identical index funds) are different ISINs with different NAVs; ask which exact plan when it's not obvious, the same way account/person was already being asked for new holdings.

**Axis Small Cap Fund — a real ~₹1 lakh data error, found and fixed via user-supplied ground truth.** The user stated the real total position (₹17.38L invested, ₹30.18L value, already inclusive of the 2026-09-05 top-up) didn't match what was on file (₹18.15L cost basis pre-top-up, ₹31.27L value). Asked for clarification before touching anything, since guessing wrong on a correction compounds the error rather than fixing it — confirmed the user's figures already included the top-up. Set `cost_basis` to the real ₹17,38,000 and `current_value` to the real ₹30,18,000 directly (both user-stated, not recomputed), then derived `units` (21,923.580) by dividing the real current_value by the fund's real AMFI NAV (137.66) — same "derive the missing side from two real knowns" convention used throughout this session's purchase updates. The ~₹1L discrepancy traces back to the original CAS transcription from early in this project and had silently survived 4 full reprice passes untouched, since reprice passes only ever update price/NAV, never re-verify the underlying unit count against an independent source.

**2 more Edelweiss Liquid Fund lots** — ₹40,000 @ NAV 3630.75, ₹10,000 @ NAV 3662.65 — added as real new capital on top of the existing position (`pos_k_mf_3`): units +11.017 and +2.7303 respectively, cost_basis +₹50,000, repriced at the fund's current AMFI NAV (3669.7301, unchanged from the prior day's pull).

**Household total**: ₹4,31,87,887.87 → **₹4,31,11,545.04** (net effect: the Axis Small Cap correction reduced current_value by ₹1,30,044.4, more than offsetting the ~₹1.09L combined new capital from the Edelweiss lots and the ICICI Direct/Regular split's small net gain). `delta_month_pct` left untouched, same reasoning as every other new-capital/correction event this week — it isn't a market move, and untangling a correction from a top-up within the same figure isn't worth the false precision.

**Verified in-browser 2026-09-06**: `node --check` clean on `positions.js` and `instruments.js`; the file's own current_value sum (₹4,31,11,545.04, 43 positions) matched the manually computed total exactly before writing `householdTotals`. Portfolio's Mutual Funds group now shows 12 positions (was 11): Axis Small Cap displays exactly ₹17.38L invested / ₹30.18L value as given; "ICICI Prudential Nifty Next 50 Index Fund" (Regular, ₹60,000 invested) and "...— Direct Plan" (₹55,744 invested) render as two distinct rows. No console errors.

## A genuinely new holding: VXUS (2026-09-05, later still)

A correction to the previous pass, not a new ask. The 5-row purchase list from the prior message used a leading "Yes/No" column that was read as "was this actually executed" — the Vanguard Total International Stock ETF (VXUS) row was marked "No" and excluded on that basis. The user clarified: "No" meant "this is not an existing item to append to" (i.e. a brand-new holding, unlike the other 4 which topped up funds already held) — not "didn't happen." It was a real purchase and needed adding as a new position, not skipped.

**Added as a new instrument + position**, not folded into anything else, since nothing like it existed. Asked which account it belongs to rather than guess (account assignment affects every member-level breakdown in the app) — confirmed **Vinod's Alpaca account**. New `inst_vxus` (ISIN `US9219097683`, verified via search rather than assumed) carries `l3: 'Foreign stock'` — not a new l3 value — specifically so it falls into the existing Foreign Equity class bucket per the user's own framing ("it's part of the foreign assets"); no `sector` field, since it's a diversified ex-US index ETF, not a single company. New `pos_v_vxus`: units 5.6471 (as given), `cost_basis` ₹47,103.26 (the user's stated $498.50 converted at the same 94.49 spot rate used for the rest of this reprice pass — no separate purchase-day FX rate was given), and `current_value` from a **real, freshly pulled Yahoo Finance quote** ($88.41, not the user's stated purchase-day price of $88.275) — same "reprice everything, don't just record a static purchase price" discipline as every other holding. `householdTotals.current_total`: ₹4,31,40,712.78 → **₹4,31,87,887.87**. `delta_month_pct` left untouched — a brand-new holding entering tracking is new capital, not a market move, same treatment as the RSU vest and the 4 SIP top-ups in the prior pass. Also updated the Goal tab's own `GOAL_FUNDS` copy, which had suggested exactly this VXUS purchase as an idea — now reads "already acted on" instead of a still-open suggestion, so the tab doesn't keep recommending something the household has already done.

**Verified in-browser 2026-09-05**: `node --check` clean on `positions.js`, `instruments.js`, and the extracted module script. Portfolio's Foreign Equity count moved 15 → 16, "All" 41 → 42; the new row renders correctly (units 5.6471, NAV 8,353.86, Avg Cost 8,341.14, Invested ₹47,103, Value ₹47,175, +0.2% gain) at the bottom of the Foreign Equity table. No console errors.

## 14-day agenda, real earnings/dividend calendar, a 4th reprice pass, and 4 new SIP purchases (2026-09-05)

Five things in one message: drop a Today action item, widen the agenda window, populate the calendar with real research, refresh every price, and record 4 real new fund purchases.

**"19 uploaded documents" dropped from Today's Immediate Actions.** The user asked what it meant, then asked it not show there — it's a disclosed build-state gap (no document-extraction pipeline exists yet, Stage 8), not something actionable today. Removed the `reviewQueue.pending_documents_uploaded` line from `portfolioActionItems()`; `pending_transactions` stays (a genuinely different, more actionable case — extracted transactions actually awaiting review — currently 0, so it doesn't show anyway).

**Agenda widened from 7 to 14 days**, plus fixed a real recurring bug while touching this code: `AgendaList`, `buildIllustrativeTrend`, and `mock-data/networth-history.js`'s sparkline anchor all had `today` hardcoded to `new Date('2026-09-02T00:00:00')` — the exact bug class flagged in this file's own "Data-integrity cleanup" entry from 2026-09-02 ("if a future session finds another hardcoded `new Date('2026-...')` acting as 'today,' treat it as the same recurring bug"), and sure enough it had crept back in. All three moved to `2026-09-05`. `AgendaList`'s window is now `Array.from({length: 14}, ...)` and the section label reads "Next 14 days."

**Real earnings/dividend calendar, researched via WebSearch — not fabricated dates.** `mock-data/calendar-events.js` (empty since 2026-09-02, when a synthetic recurring-SIP prediction was removed) now holds 12 real, sourced entries: confirmed or company-announced dates where available (Micron's fiscal Q4 earnings via their own IR press release, Broadcom's dividend payment date, Microsoft/Alphabet/AMD/ASML/ServiceNow's confirmed earnings dates, Apple's declared dividend ex-date), and a smaller number of analyst/pattern-based **estimates** explicitly marked `confirmed: false` with the reasoning disclosed (TSMC, Meta, Astera Labs, NVIDIA — none had company-confirmed dates as of this pull). Every entry carries a `source` field. **Deliberately excluded**: all 10 Indian stock holdings, Marvell, and the AB InBev RSU — none had a reliably sourced date (Indian companies typically only announce the exact board-meeting date about a week out), a disclosed gap rather than a guess. `amount` is `null` on every pure informational date (an earnings call isn't a cash-flow event) except Apple's dividend, where it's a real computed number — the household's actual 10.4596 units × $0.27/share × spot USD/INR, not the per-share figure itself. Fixed two rendering call sites (`AgendaList`, `whatChangedCards`) that unconditionally ran `formatINR(e.amount)` — would have shown a misleading "₹0" for every null-amount event — to skip the amount display when `null`.

**Fourth full reprice pass (2026-09-05)** — identical two-source method as the three before it (AMFI `NAVAll.txt` by ISIN for the 11 MF positions, Yahoo Finance's chart API by ticker for all 26 stock/RSU/gold positions plus USD/INR). AMFI's freshest file still only had 04-Sep NAVs published (the normal one-day publication lag — 05-Sep NAV wasn't out yet at pull time), disclosed in `PRICE_ASOF` rather than presented as same-day. `tech` blocks (52-week range, day range, volume) refreshed on every stock/RSU instrument too.

**4 real new SIP purchases folded into existing positions, not new rows.** The user listed 5 line items from what looks like their own tracking sheet (columns: confirmed Yes/No, fund, person, NAV date, invested amount, units, NAV) dated 2026-09-05. 4 were marked "Yes" and matched instruments the household already holds under the same person — so units/cost_basis were added to the *existing* row (`pos_v_mf_1`, `pos_k_mf_2`, `pos_k_mf_5`, `pos_k_mf_6`), the same multi-lot convention already used for Keerthana's two Parag Parikh folios and the second RSU vest, rather than creating duplicate rows for the same holding. Each entry gave either the amount or the unit count but not both; the missing side was computed against that fund's actual AMFI NAV (04-Sep, the freshest available) — real arithmetic, not invented numbers. The 5th item (Vanguard Total International Stock ETF, ~$498.50 / 5.6471 units) was marked **"No"** in the user's own sheet — misread at the time as "not actually executed," so it was left out of the fixtures. **Correction, same day, see "A genuinely new holding: VXUS" below**: "No" actually meant "not an existing item to append to," i.e. a real, brand-new purchase — it was added afterward as a new instrument/position, not skipped. `householdTotals.current_total`: ₹4,28,98,410.44 → **₹4,31,40,712.78** (this pass; VXUS added ₹47,175.09 more in the follow-up correction). `delta_month_pct` computed net of the ₹1,72,738.38 of brand-new capital (same "new money isn't a market move" principle used for the RSU vest and real estate/gold's first-tracked values) — real market move was **+0.16%**.

**Verified in-browser 2026-09-05**: `node --check` clean on `calendar-events.js`, `positions.js`, `instruments.js`, and the extracted module script; div/span/button tag-balance all delta-0. Today shows "Next 14 days," correctly reports "Nothing on the calendar these two weeks" (none of the 12 real calendar dates fall in this particular 14-day window — earnings season clusters in Oct/Nov, an honest result, not a bug) and the What Changed rail correctly surfaces the nearest real event (Broadcom's dividend date) once the hardcoded "today" was fixed. Portfolio shows the updated units (HDFC BSE Sensex Direct: 7,453.36 → 7,517.51; Axis Small Cap: 22,701.19 → 22,868.26) and the new household total. No console errors on Today or Portfolio. (Needed a hard-reload during testing — Chrome had cached the unchanged-URL ES module imports across the dev-server restart, a known local-testing quirk, not an app bug.)

## Portfolio: Invested column, real invested-to-date feeds the Goal chart (2026-09-04, later still)

Follow-up to the Avg Cost column added earlier today: "now u have the average cost... also add the [invested] value... and adjust in the graph in goal."

**Invested column.** New `realCostBasis(p)` — `cost_basis`, or `cost_basis_usd × PRICE_ASOF.usd_inr` — factored out of `gainPctOf()` (which already computed the identical thing inline) so both the Gain % column and the new Invested column trace to the exact same real number, not two separately-written copies that could drift. Unlike Avg Cost (deliberately native-currency, per-unit, to match the NAV column), Invested is always INR — matching the Value column's convention, since it's a total monetary figure meant to be glanced at side-by-side with Value. Rows with no real cost basis on file (both AB InBev RSU lots) show "—", same honesty rule as Avg Cost. Group-header rows (the "All" grouped view) now also sum a real per-group invested total instead of leaving that cell blank.

**Real invested-to-date now feeds the Goal chart's "Today" bar.** New household-level constant `GOAL_INVESTED_TO_DATE = ALL_PRICED.reduce((s,p) => s + impliedCostBasis(p), 0)` — deliberately reuses `impliedCostBasis()` (the same function `ScopeHeroCard` already uses for its own "Invested ₹X" line in Portfolio/Insights), not the new table's stricter `realCostBasis()`, because a household-level aggregate needs a number for *every* position including the two RSU lots with no cost basis on file — `impliedCostBasis()`'s existing 0.85-of-current-value RSU fallback is the established, already-accepted way this codebase handles that case at the aggregate level (the per-row Invested column stays honest with "—" instead, since a row-level number reads as more precise/certain than an aggregate). This fixed a real inaccuracy in `gpYearSeriesWithPrincipal`: the "Today" bar previously treated the *entire* current net worth as "invested, zero growth" — technically wrong, since today's ₹4.29 Cr already includes real unrealized gains from money invested years ago. Now Today's bar splits into ₹3.20 Cr real invested principal (bronze) and ₹1.09 Cr already grown (sage), and the `principal` accumulator that future years walk forward from starts at that same real figure instead of the inflated placeholder — every subsequent year's invested/growth split shifts correspondingly closer to reality. The chart's legend and note text were updated to state the real Today split explicitly rather than leaving it implicit.

**Verified in-browser 2026-09-04**: `node --check` clean on the extracted module script; div/span/button/td/tr tag-balance all delta-0. Portfolio's Mutual Funds group header now shows a real ₹1.27 Cr invested vs. ₹1.75 Cr value; individual rows (e.g. HDFC BSE Sensex Index Fund: ₹44.07L invested vs. ₹54.35L value) match Avg Cost × Units to the rupee. Goal's Today bar shows ₹3.20 Cr invested / ₹1.09 Cr growth = ₹4.29 Cr, consistent with the household hero and every later year's bar total. No console errors.

## Portfolio: default-All, Avg Cost column, Gold rename (2026-09-04, later still)

Three small Portfolio-tab asks in one message.

**Real Assets → Gold.** `CLASS_BUCKETS`' `real_assets` entry's `label` changed from `'Real Assets'` to `'Gold'` — the bucket has only ever held the one HDFC Gold ETF position, so the generic label was needless indirection. Since every screen that shows class names (Portfolio's buttons/table-group-headers/allocation-bar-row, Insights' class buttons, `classComparisonData`'s cross-class-comparison bars) reads the label from `classLabel()`/`CLASS_BUCKETS` rather than a hardcoded string, this one-line change propagated everywhere automatically — no separate edits needed per screen. The bucket's internal `id` (`real_assets`) was left unchanged, since it's not user-visible and renaming it would've meant touching every reference to that id across the file for no visible benefit.

**Avg Cost column added to the holdings table.** New `avgCostOf(p)` — `cost_basis / units` (or `cost_basis_usd / units` for Keerthana's DriveWealth rows, left in native USD to match how the existing NAV column already stores `price_usd` natively for those same rows, rather than converting to INR). Deliberately reads the *real* `cost_basis`/`cost_basis_usd` fields only, never `impliedCostBasis()`'s RSU/no-data fallback (that function exists for return-percentage math elsewhere, where a rough estimate is an acceptable approximation; a literal "Avg Cost" column showing a fabricated number would be a different, worse claim) — rows with no real cost basis on file (both AB InBev RSU lots) correctly show "—" rather than a made-up figure, consistent with the AI-boundaries rule against generating a UI number with no real code path behind it. Column sits between NAV and Value; sortable like every other column.

**Default class filter changed from Foreign Equity to All.** `PortfolioScreen`'s `classId` state now initializes to `'all'` instead of `'foreign_equity'` (that default dated back to Portfolio v3's original spec, 2026-08-03, and was never revisited). The user asked for it to always default to All going forward.

**Verified in-browser 2026-09-04**: `node --check` clean on the extracted module script; div/span/button/td/tr tag-balance all delta-0 (the pre-existing `th` +3 artifact, from the regex not matching dynamic `<th key=...>` template attributes, confirmed unchanged). Portfolio now opens on "All" with the grouped table; class buttons and the asset-class bar row both read "Gold" instead of "Real Assets"; Avg Cost shows real values (e.g. HDFC BSE Sensex Index Fund ₹591.34, NVIDIA ₹5,792.93) and "—" for both RSU rows. No console errors.

## Split-bar full-width fix, and a third full reprice pass (2026-09-04, later same day)

**Split bar widened to full width, aligned with the chart above.** The `SplitBar` added earlier today sat in a plain (non-`span-2`) column of the Goal tab's `.insights-col` masonry grid, so it rendered at ~440px next to whatever else was in that column — narrower than the `.span-2` "Year-over-year" chart card directly above it. Added `class="span-2"` to that section's wrapping `<div>` (the same mechanism already used by the chart card, the funds list, and the purchasing-power section) so it now spans the full content width and left-aligns with the chart above it, per the user's explicit ask.

**Third full reprice pass — same two-source method as the two prior passes, no new technique needed.** The user asked to "refresh the NAVs and Stock prices across." Re-ran the identical method documented in the `mock-data/positions.js` header: AMFI's `NAVAll.txt` (curl'd directly with a browser `User-Agent`, dated 03-Sep-2026 — AMFI publishes T-1 by early morning of T) grepped by all 10 unique MF ISINs, and Yahoo Finance's `v8/finance/chart/<TICKER>` endpoint (curl'd per-ticker, also with a `User-Agent` header — the newer `v7/finance/quote` batched-symbols endpoint returned `401 Unauthorized` this time, so stayed with the per-ticker chart endpoint that has worked every prior pass) for all 26 stock/RSU/gold-ETF positions plus the live USD/INR rate (94.485, was 94.908). Every position's `current_value`/`unrealized_gain`/`nav`/`nav_asof` updated; the `tech` block (52-week range, day range, volume, `asof`) on every stock/RSU instrument in `instruments.js` refreshed too, same as the 2026-08-02 pass that first added those fields.

`householdTotals.current_total`: ₹4,25,90,958.38 → **₹4,28,98,410.44**. `delta_month_pct` computed carefully around the second RSU lot added just two days ago: measured as (sum of all positions repriced **excluding** `pos_v_rsu_2`) vs. the last full-reprice baseline (₹4,11,56,535, from 2026-09-02) — **0.7148%**, not the naive (new total − last-shown total) which would have wrongly folded the new vest's ₹14.48L into a "market move." Verified the total three ways before writing it into any file: the per-position calc, a direct regex-sum over the written `positions.js`, and a manual cross-check that `sum − pos_v_rsu_2.current_value` matched the delta baseline — same discipline as every prior reprice pass. Both `PRICE_ASOF` and the file's own header comment (which had drifted to "20 stock/RSU/gold-ETF positions" even before this pass — the real count was already 25/26, not 20/21; corrected to the actual count this time) were updated.

**Verified in-browser 2026-09-04**: `node --check` clean on the extracted module script, `positions.js`, and `instruments.js`; div/span/button tag-balance all delta-0. Household Today on the Goal tab reads ₹4.29 Cr; Portfolio's Foreign Equity table shows the repriced NVIDIA NAV (₹21,585.10) and both RSU lots at the new NAV (₹7,579.59); Real Assets bar moved to ₹35.00L. No console errors on Goal or Portfolio.

## Goal chart font fix, a second RSU vest, and a single split bar replacing a donut (2026-09-04)

Three small fixes/asks in one message.

**Goal chart's "elongated font" bug — a real SVG scaling bug, not a subjective style complaint.** `GoalGrowthBars` (added 2026-09-03) used `viewBox="0 0 720 240"` with `width="100%"` and a fixed pixel `height`, plus `preserveAspectRatio="none"`. On a `.span-2` card that actually renders ~1100-1180px wide, that stretches the X axis by ~1.6x relative to Y (non-uniform scale) — every glyph and bar rect gets stretched horizontally by the same factor, which is exactly what "elongated, not professional" looks like. `YearChart`/`TrendChart` elsewhere in this file use the same `w=720`/`preserveAspectRatio="none"` pattern and have the identical latent distortion, but it's far less visible on a plain line chart with minimal axis text — GoalGrowthBars is unusually text-heavy (a value label per bar, per segment), which is what made it visible. **Fix, scoped to this one chart** (didn't touch the shared `YearChart`/`TrendChart` — out of scope, no complaint raised about them, and changing shared components risks regressions elsewhere without being asked): widened the internal viewBox to `w=1080` so the scale ratio is close to 1:1 for a real ~1100-1180px card, bumped `height` 240→280 and every font-size up (9-11px true size now, since they're no longer being artificially magnified by the old distortion), and increased `rx` slightly on the bar rects to match. **Lesson for any future inline-SVG chart in this file**: `preserveAspectRatio="none"` with a viewBox width far from the real rendered width silently stretches text — check the real container width against the viewBox width before shipping a chart with meaningful on-chart text, not just axis labels.

**Second AB InBev RSU vest.** The user reported a new, real vest — 191 units, dated today — same instrument as the existing 255-unit lot (`pos_v_rsu`). Added as a second position row, `pos_v_rsu_2` (same multi-lot-per-instrument pattern already established for Keerthana's two Parag Parikh Flexicap folios, `pos_k_mf_7`/`pos_k_mf_8`), same last-known price (79.13 USD, no fresh Yahoo pull this pass) and same `cost_basis: null` as the first lot (no vest-date FMV supplied either), with its own `vest_date: '2026-09-04'` field. `householdTotals.current_total`: ₹4,11,56,535 → **₹4,25,90,958.38**. `delta_month_pct` deliberately left untouched — a new vest entering tracking is new shares, not a market move, same reasoning already applied to real estate/gold's first-tracked values. `GOAL_PLAN.marketExisting` picked the new total up automatically (it's derived from `householdTotals.current_total` minus the FD/EPF/gold buckets, never hardcoded), so the Goal tab's numbers moved correctly with no separate edit. *(The "no fresh Yahoo pull this pass" caveat above was true when this vest was added — a full reprice pass later the same day, see the section above, did subsequently reprice both RSU lots along with everything else.)*

**"Next 6 months" donut replaced with a single split bar.** The user asked for "a single line split with % of ur suggestions and colors" instead of the small donut+legend. New generic `SplitBar` component — one continuous pill `<div>` divided into `flex: weight` colored segments (same `GOAL_ALLOCATION` data, same colors), with a wrapped legend row of dot + label + bold % underneath. Simpler and more compact than the donut it replaced; general-purpose enough to reuse anywhere else a 3-5-item % split needs to be legible in one glance rather than a full ring chart.

**Verified in-browser 2026-09-04**: `node --check` clean on the extracted module script; div/span/button/g/svg/rect tag-balance all delta-0. Confirmed via screenshot: Goal chart text now renders at a normal, non-stretched aspect ratio; Household Today correctly reads ₹4.26 Cr; Portfolio's Foreign Equity list shows both AB InBev RSU rows (255 and 191 units) and the class count went 14→15; the split bar renders as one bar in 4 real colors (45/20/20/15%) with a labeled legend. No console errors on Today, Portfolio, or Goal.

## Today actions, Portfolio class-allocation bars, and a Goal YoY bar chart (2026-09-03)

Three independent asks in one message, each touching a different screen.

**Today: "Needs You" replaced with real Immediate/Future actions.** The user asked, instead of the old static "Needs you" card (review-queue backlog + coverage gaps only), for "based on my current portfolio, can you tell me the immediate actions i need to take... or any future actions." `NeedsYou()` was removed entirely and replaced by `portfolioActionItems()` + `PortfolioActions` — a real, computed split into two cards, reusing existing computed-insight functions rather than inventing new advisory logic (AI boundaries: never recommend a buy/sell on a specific instrument, never generate a UI number with no code path behind it). **Immediate** = something with an actual decision or backlog attached right now: open High/Critical-severity flags (`flags.js`), unprocessed review-queue items. **Future** = worth watching, nothing forces a decision today: concentration flags (`concentrationFlagsForClass`, ≥8% of net worth), the nearest FD maturity (`maturityLadderData`), which of the 3 loans has the smallest invest-vs-prepay spread (reusing the same base-case-SIP-vs-loan-rate math already in the Debt tab), a real MF rebalancing note (`mfRebalancingBullets`), and the existing coverage gaps. Every item traces to a real function already used elsewhere in the app (Insights/Debt tabs) — nothing new was fabricated, this pass just re-surfaces those same computations on Today instead of leaving them buried one tab deep.

**Portfolio: a "By asset class" horizontal-bar row, and a grouped-table fix.** New `classAllocationData()`/`ClassAllocationBars` render a bar per `CLASS_BUCKETS` entry (MF/Indian Equity/Foreign Equity/EPF/Fixed Income/Real Assets) with real value + % of total, member-filter-aware, placed between the two hero cards and the class-button row — direct answer to "below the two big numbers, show a horizontal bar with each class of investment both value and %." While building this, found the actual "showing Equity" complaint: `HoldingsTable`'s "All" grouped view was grouping by `groupByL1` (`instruments.js`'s coarse L1 field), which lumps mutual funds + Indian stocks + foreign stocks/RSU all under one "Equity" header — a different, coarser grouping than the class buttons above it use. Added `groupByClassBucket()` (same shape as `groupByL1`, but keyed by `classOf(p)`/`classLabel()`) and switched the "All" table view to it, so the grouped table now shows the same MF / Indian Equity / Foreign Equity split the class buttons and the new bar row already used — one consistent taxonomy across the screen instead of two.

**Goal: the 3-scenario line chart replaced with a year-over-year invested-vs-growth bar chart.** The user wanted "bars on YOY evolution with values.. how much is invested and how much is interest from today YOY," not a projection line. New `gpYearSeriesWithPrincipal(rates)` walks the same month-by-month loop `gpYearSeries` already uses, but additionally accumulates real cumulative principal (starting corpus + every SIP/EPF/May-lumpsum contribution actually made by that point) alongside the total value — growth is computed as `value - principal`, never a separately-modeled number, so it can't drift from the real total. New `GoalGrowthBars` (hand-rolled SVG, matching the existing `YearChart`/`TrendChart` style) renders 11 stacked bars (Today + 2027–2036), bronze = invested, sage = growth, with compact `formatINR` value labels on the total and inside each segment where there's room. Since 3 scenarios × 2 stacked segments × 11 years doesn't fit legibly in one chart, added a scenario toggle (`Optimistic`/`Base case`/`Pessimistic` buttons, reusing the existing `.class-btn` style) defaulting to Base case — the scenario-comparison stats row and assumptions note below the chart (all 3 scenarios' final values) were kept as-is, unaffected by the swap.

**Verified in-browser 2026-09-03**: `node --check` clean on the extracted module script; div/span/button/svg/table/tr/td/select/option tag-balance all delta-0. Loaded via a local `python -m http.server`, walked Today (Immediate actions correctly surfaces the real STRUCT-02 High flag + the 19-document review-queue backlog; Future actions correctly surfaces HDFC BSE Sensex/EPF concentration, the FD maturing Feb 2028, and the closest-to-competitive loan), Portfolio (asset-class bars show real ₹1.76Cr/42.7% MF down to ₹22.81L/5.5% Indian Equity; "All" grouped table now shows a "FOREIGN EQUITY" header distinct from "MUTUAL FUNDS" instead of one lumped "Equity"), and Goal (YoY bars render with correct per-year invested/growth split, 2036 base-case total ₹18.97 Cr matches the existing hero card and stats row exactly; switching to Optimistic correctly re-renders to ₹29.30 Cr, matching its own stat). No console errors on any screen.

## Purchasing-power reality check + FD alternative, in the Goal tab (2026-09-02, later still)

Follow-up to a "let's discuss ideas" conversation (research on VPF, NPS, FX tailwind, tax-loss harvesting — discussed in chat, not built) where two of the ideas got greenlit for the Goal tab specifically.

**Purchasing-power reality check**: new `INFLATION_SCENARIOS` (optimistic 4% / base 5% / pessimistic 6%, based on real research — India CPI running ~4–4.5% currently, 5.6% average over the prior decade) applied to the fixed ₹20 Cr goal, deflated 10 years. Base case: ₹20 Cr in 2036 ≈ **₹12.28 Cr** of today's purchasing power; to have today's ₹20 Cr power in 2036 would need a nominal target closer to **₹32.58 Cr**. Deliberately a *separate axis* from the existing optimistic/base/pessimistic *return* scenarios (investment-return uncertainty vs. purchasing-power uncertainty) — not cross-multiplied into 9 combinations, just applied once to the fixed goal amount, to keep the section legible.

**"If it all sat in a 5% FD instead"**: the user's own ask — what would ₹20 Cr in a plain 5% FD hand over monthly, after tax, and what's that worth in today's money. Real tax research behind the number: FD interest is "income from other sources" (not the equity LTCG/STCG rates used everywhere else in this app), taxed at slab. Computed the actual applicable band rather than guessing: the household's stated income (₹60.72L/yr from the Budget tab) plus this FD's ₹1 Cr/yr interest totals ~₹1.61 Cr/yr, landing in the ₹1–2 Cr surcharge band (15%), not the >₹2 Cr band (25%) — new tax regime FY2026-27 slab (30% above ₹24L) × 1.15 surcharge × 1.04 cess ≈ **35.88% effective**. ₹20 Cr @ 5% → ₹8,33,333/month pre-tax → **₹5,34,333/month post-tax** → **₹3,28,034/month in today's purchasing power** (base-case inflation). Disclosed clearly as illustrative, not a tax return — actual liability depends on whose name the corpus sits in and what else that person earns that year; this is the household's own stated income used as the only defensible anchor, not a guess.

**Verified before writing into the UI**: computed both sections independently in Node, matched the in-page JS output before committing (same discipline as every prior data-driven pass). `node --check` and the tag-balance delta (same persistent +7 self-closing-div artifact, unchanged) both clean.

## Real estate excluded from net worth, tab reorder, a second Budget correction, budget insights (2026-09-02, later still)

Four things in one follow-up message, right after the real estate/Debt tab pass above:

**1. Real estate reverted out of the wealth equation entirely — not just out of one bucket.** The user was explicit: "keep this outside the overall wealth equation... I want to achieve 20Cr on top of this." This is a different, stronger requirement than the total-vs-liquid split built in the previous pass (which still counted real estate in `current_total`, just labeled it separately in the Goal tab). Full reversal, not a relabel: `householdTotals.current_total` back to **₹4,11,56,535** (pre-real-estate). `GOAL_PLAN` lost its `realEstate`/`realEstateRates` bucket entirely — `gpTotal`/`gpYearSeries`/`gpMonthsToGoal` reverted to their pre-real-estate signatures (no `scenarioName` parameter), and the Goal tab's total-vs-liquid stat rows/copy reverted to a single figure.

**Architectural fix, not just a number revert**: real estate was moved OUT of `positions.js`/`instruments.js` entirely into a new standalone `mock-data/real-estate.js`, rather than merely being excluded from one total. The reasoning: if real estate stayed as `positions` entries (even with `current_total` computed to exclude them), any other screen that independently sums `positions` for its own totals — Portfolio's own subtotals, Insights' allocation math — could still silently pick them back up, since those screens don't reference `householdTotals.current_total`, they sum the array themselves. Keeping real estate fully outside `positions` is the only way to guarantee it can't leak in anywhere. `DebtScreen` now imports `{ realEstate }` from the new file directly instead of filtering `positions` by account. **Lesson for future asset classes**: if something needs to be excluded from net worth, ask whether "excluded from the total" or "excluded from the tracked portfolio entirely" is meant — they're different, and only the second one is leak-proof if other screens compute their own sums from the same array.

**2. Tab renamed and reordered, both explicit.** "Debt" → **"Debt & Immovable Assets"** (now that it's explicitly the one place real estate lives). Order set to **Today / Portfolio / Debt & Immovable Assets / Budget / Insights / Goal** — a deliberate user choice, not alphabetical or build-order; see the "Nav lives in a top bar" convention note for the standing instruction not to re-sort this.

**3. A second Budget correction — the ₹7,000 line is parents' health insurance, not car insurance.** The first correction (same day, see the reprice/data-integrity entry above) had already pulled "Insurance (savings-linked)" and "Car insurance (savings-linked)" out of the `investment` category into their own `insurance` bucket, guessing both were car-insurance-related. The user then clarified the ₹7,000 line specifically is parents' health insurance premium. Recategorised again: that line moved to `family` (alongside the parents transfer — ₹27,000/month combined, a real quantified family-support figure), and the remaining ₹1,700 car-insurance line folded into `essential` (too small to justify its own category once it stood alone) — the standalone `insurance` category was removed from `CATEGORY_META`. Real savings rate (46.25%, MF SIP only) was unaffected by this second correction, since neither line was ever counted as investment after the first fix.

**4. Automatic budget-health insights, in direct response to "are we spending a lot, how can I improve."** New `budgetHealthInsights()` (module-level, computed live — never a hardcoded verdict) renders 5 cards in the Budget tab: savings-rate benchmark (46.25% vs. commonly-cited 20%/30% thresholds — "exceptional"), EMI-to-income ratio (26.1% vs. the ~30–40% lending-guidance ceiling — "within comfortable range"), the largest genuinely discretionary line (Travel, ₹50K/month — framed as "the honest answer to where flexibility would come from," not a suggestion to cut it), the real family-support total (₹27K/month, 5.3% of income), and essential-living leanness (11.9% of income — "no sign of lifestyle inflation on the needs side"). Every card states the real number and a benchmarked read of it, then stops — no specific action recommended, consistent with the AI-boundaries line already established for the Debt tab's debt-vs-invest cards.

**Two real syntax bugs caught before commit, same root cause as one from the previous pass**: stray backticks used as inline-code formatting inside htm template-literal strings (`` `investment-plan-20cr.html` ``, then later `` `householdTotals.current_total` ``) broke the outer template and failed `node --check` both times. Backticks are never safe as literal text inside an htm/JSX template string in this file — use plain text or a different marker, never `` ` ``. Both `node --check` and the tag-balance delta check (same persistent +7 self-closing-div artifact, unchanged) came back clean before the final commit.

## Debt tab, real estate, and a Budget correction (2026-09-02, later still)

The user pasted a screenshot of 3 real loan accounts (principal outstanding, remaining term, rate — account numbers masked to last 4 digits before anything was written to disk, since this repo is public) and described 2 real properties: Nikoo Homes 1, Bangalore, Unit 100704 (2104 sqft, mortgaged — the 3 loans), and Nikoo Homes 4, a 465 sqft studio, explicitly loan-free. Both got current market-value estimates from the user's own stated ₹/sqft rates (₹13,500 and ₹12,500 respectively) — not an independent valuation, disclosed as such. Also: a correction — "Insurance (savings-linked)" and "Car insurance (savings-linked)" in the Budget tab were miscategorised; the user clarified both are car-insurance-premium sinking funds, not investment. Recategorised to a new `insurance` bucket — **real savings rate corrected from ~48% to 46.25%** (MF SIP only, the more defensible figure anyway).

**Cross-check done before building anything on the loan data**: computed each loan's implied EMI from its own principal/rate/remaining-term (standard amortization), summed to ~₹1,30,314/month — within 1.3% of the real ₹1,32,000 "House loan EMI" already in the Budget tab. Close enough (rate/term rounding) to treat as the same real number confirmed two ways, not two disagreeing figures needing reconciliation.

**Real estate added to `positions.js`/`instruments.js`** as a new `Real Estate` L1, under a new `acc_household_realestate` account (ownership attribution unconfirmed — filed under Vinod, same convention as the FDs, pending clarification). Follows the existing "units × nav = current_value, checkable" convention: `units` = sqft, `nav` = the user's stated rate per sqft. `cost_basis`/`unrealized_gain` are `null` throughout — no purchase price/date on file, same convention as the RSU, nothing fabricated. `householdTotals.current_total`: ₹4,11,56,535 → **₹7,53,73,035**, purely from real estate entering tracking for the first time — **`delta_month_pct` deliberately still excludes real estate from both sides of the comparison**, since counting it would misrepresent "we started tracking a new asset" as an implausible monthly gain. Liabilities live in a new `mock-data/liabilities.js`, not netted against `current_total` anywhere except inside the new Debt tab itself (which shows gross real estate value, net equity, and total debt as three separate, clearly-labeled numbers) — changing what `current_total` *means* everywhere else in the app (Portfolio, Insights, Today) was judged out of scope for one pass; noted as a live architectural question, not resolved.

**GOAL_PLAN got a new bucket, not a silent inflation of an existing one** — real estate carved out with its own scenario rates (optimistic 10% / base 6% / pessimistic 2%, base case anchored to a real researched figure: "5–7% annually going forward" is the forward-looking consensus for Bangalore residential, explicitly *not* the ~12%/yr seen over the hot 2020–2025 window). This is the exact same class of fix already applied to gold earlier the same day — a new asset entering `current_total` must get its own bucket/rate in `GOAL_PLAN`, never fall through into the equity-scenario "market existing" bucket by omission.

**The real estate addition changed the Goal tab's headline story enough that it needed a second visible number, not a silent total swap**: under base case, *total* wealth (real estate included) now clears ₹20 Cr comfortably — 126% of goal by year 10, crossing around year 7.9. But ~₹3.42 Cr of that starting point is an illiquid house, not investable capital. So every scenario in the Goal tab now shows **total** and **liquid (ex-real-estate)** side by side — total answers "how is net worth doing," liquid answers "is the investment plan itself still working." The liquid figure barely moved from before real estate existed (still needs ~₹2.78L/month, up from ₹2.34L, to land exactly on ₹20 Cr in 10 years under base case) — a house's appreciation was never going to fund that gap in spendable/investable terms, and the tab says so explicitly rather than letting the total number quietly imply the investing question was solved.

**Debt tab** (`DebtScreen`, new nav item after Budget): the 3 loan accounts, the 2 properties (sqft × rate shown transparently), and a debt-vs-invest comparison — each loan's real rate against the Goal plan's own new-SIP scenario rates (14.3% / 9.1% / 3.35%), framed as "the math," never a recommendation to prepay (AI boundaries: never recommend a specific financial action). Base case favors continuing to invest for 2 of 3 loans (7.1%, 7.65%); the smallest, highest-rate loan (9.3%, ₹2.17L outstanding) is the one where prepaying is closest to competitive — flagged as "closest," not "do this." Explicitly notes Section 24(b) home-loan interest tax deduction isn't factored in (depends on tax slab/occupancy, not computed) — a real factor that would tilt every comparison further toward investing.

**Verification before publishing**: computed the debt-vs-invest spreads, real estate market values, and the loan-vs-EMI cross-check independently in Node before writing them into any UI copy. Caught one real bug while editing — a stray backtick inside a template-literal string (`` `investment-plan-20cr.html` `` used as inline code formatting) broke the outer htm template and failed `node --check`; fixed by removing the backticks. `node --check` and the tag-balance delta check (still the same pre-existing +7 self-closing-div artifact, confirmed unchanged by this pass) both came back clean before commit.

## Budget tab (2026-09-02, later still) — the first real income figure Mina has ever had

The user pasted one representative month's real household income/outflow breakdown directly in chat and asked for it as its own tab (not a revival of the hidden Money tab — genuinely new, Money stays hidden). Stored in `mock-data/budget.js` (`monthlyBudget`, `CATEGORY_META`), rendered by `BudgetScreen` (new 6th nav item, "Budget", between Insights and Goal).

**What the numbers say, verified by script before building anything on top of them**: income ₹5,06,000; outflows sum to exactly ₹5,06,000 (zero unaccounted, assuming the one blank line — AMC/cleaning, no amount given — is ₹0). Real savings rate: **47.96%** if savings-linked insurance premiums count, **46.25%** if only the equity SIP counts — the first time this app has had a real, computed savings-rate figure instead of the "not computable, no bank statement" gap-note that's been in Money/Insights since the first build. The MF outflow (₹2,34,000) is the exact same number as the Goal tab's monthly SIP — confirmed as the same real figure in both places, not a coincidence requiring reconciliation.

**Two things surfaced that weren't visible before**: (1) the household invests **literally 100% of its monthly surplus already** — the ₹2,34,000 SIP isn't idle-money-that-could-be-larger, it's the entire gap between income and every other line item. The Goal tab's "need ₹48K/month more for base-case 10-year timing" finding now has a real answer to "from where" — it has to come from cutting a category (Travel/Subscriptions are the only genuinely discretionary ones; EMI and the parents transfer are fixed) or from income growth, not from slack that was hiding somewhere. (2) The ₹1,32,000 **House Loan EMI implies a real home loan that isn't tracked as a liability anywhere in net worth** — no principal, outstanding balance, or property value is on file. Flagged as a gap in the screen itself, not fabricated or silently ignored.

**Cross-reference, both directions**: Budget's own callout card computes the Goal tab's required-SIP gap live (reuses `gpTotal`/`GOAL_PLAN.scenarios.base`, not a re-typed number) so the two screens can't drift apart the way the earlier hardcoded-duplicate bug did. The Goal tab's own "what actually moves the outcome" card now points back to Budget for where the money would come from.

**Not yet done**: no month-over-month trend (this is one snapshot, not a series — the user said "this is one month," implying more may follow); AMC/cleaning amount is genuinely unknown, not assumed zero in the underlying data (only in the balance-check math); the home-loan liability gap is noted but not filled (would need principal/rate/property value, none of which exist yet).

## Data-integrity cleanup (2026-09-02, later same day) — a real bug and a real decision

The user caught that the Gold ETF hadn't actually been repriced in the reprice pass above (it was correctly *disclosed* as stale, but that's not the same as fixed) and asked what a confusing agenda entry — "HDFC index fund purchase on Thursday" — actually was. Both turned out to be real problems, not just stale-data housekeeping:

1. **Gold ETF, now genuinely live**: verified against Yahoo Finance's own quote for the HDFC Gold ETF ticker itself (`HDFCGOLD.NS`, ₹127.80), not a proxy. `pos_k_gold.current_value`: ₹32.36L → **₹34.03L**. `current_total`: ₹4,09,90,401 → **₹4,11,56,535**. `delta_month_pct` recomputed accordingly (now 1.12%, still the real Aug→Sep move, just complete this time).
2. **A real bug, not just stale data**: `calendarEvents` in `calendar-events.js` had a synthetic "next SIP purchase" prediction (`evt_1`, invented date/amount, never sourced from a real standing-instruction feed) that the Today screen's `AgendaList`/`whatChangedCards` were reading against a **hardcoded `today = new Date('2026-08-02...')`** — so a month-old fake event kept surfacing as "this week's" agenda indefinitely. Fixed both: `calendarEvents` is now genuinely empty (comment explains why — don't repopulate it with a guessed recurrence, only real dated/sourced events), and the hardcoded date anchors in `AgendaList`, `whatChangedCards`' `nextEvent` path, and `buildIllustrativeTrend` all moved to `2026-09-02`. **This exact hardcoded-stale-date pattern already bit `networth-history.js`'s sparkline anchor once this same day** (fixed earlier, see the reprice-pass entry above) — it's now bitten twice. If a future session finds another hardcoded `new Date('2026-...')` acting as "today" anywhere in `index.html`, treat it as the same recurring bug class, not a one-off.
3. **Found while fixing #1**: `GoalPlanScreen`'s `GOAL_PLAN` object had its own hardcoded copies of the FD/EPF/Gold totals (duplicated from `positions.js` instead of imported), which had already silently drifted out of sync with the gold reprice — the error would have misattributed the gold ETF's price move into the "market-linked existing" scenario bucket instead of gold's own fixed-rate bucket. Fixed by deriving `GOAL_PLAN.fd`/`epfExisting`/`gold` live from `positions` by account/id lookup instead of hardcoding. **Lesson: any duplicate of a real number outside its source of truth is a future bug, even a "just this once" constant** — this is the second time in one day a hardcoded duplicate caused a real discrepancy (see `investment-plan-20cr.html`'s own hardcoded totals, which still need manual updates on every reprice since it has no shared import with `mock-data/` — a known, accepted limitation of the standalone-file approach, not fixed the same way).
4. **Money tab hidden from nav** (explicit user request, not a bug) — see the Nav decision table and the "Nav was frozen..." convention note below for what changed and how to reverse it.

## Goal tab (2026-09-02) — a ₹20 Cr, 10-year household growth plan

Built after a direct ask: "I want to reach 25Cr in 10 years... where would you invest... do scenario planning... add a separate sheet." Delivered two artifacts from one shared model: a standalone researched document (`investment-plan-20cr.html`, also published as a Claude Artifact) with full citations, and — after a same-day follow-up — a live in-app screen (`GoalPlanScreen`, new 5th nav item "Goal").

**The follow-up changed three real inputs**, all reflected in both places: (1) the goal dropped from ₹25 Cr to ₹20 Cr, (2) EPF contributions (₹34,700 employee + ₹34,700 employer = ₹69,400/month, flat, no salary step-up modeled) were added as a real cash flow into the EPF bucket rather than just the passive existing balance, (3) an annual ₹10,00,000 lumpsum every May (first: May 2027) was added, invested in the same diversified allocation as the monthly SIP. Together these meaningfully closed the gap to goal — base case went from "~₹16 Cr, ~13.8 years" (under the old ₹25 Cr / no-EPF-contribution model) to "~₹18.86 Cr, ~94% of goal, full goal at ~10.5 years" — a much more achievable story once the real EPF contribution and lumpsum were counted.

**Methodology** (same in both the sheet and the in-app screen — they share one model, just reimplemented in each file's own idiom since the app has no shared module system across index.html and standalone reports): current net worth (₹4.07 Cr) split into FD (contracted ~7.5%), EPF existing balance (statutory ~8.25%), Gold (~8% long-run trend, existing holding untouched by new money), and market-linked existing holdings (MF/stocks/US/RSU) — only the last one and all new money (SIP + EPF contributions + May lumpsum) flex with the scenario. Three scenarios (optimistic/base/pessimistic) with bottom-up CAGRs: existing corpus 17%/10%/2% (wide spread — real AI/semis + smallcap concentration already in the household), new money 14.3%/9.1%/3.35% (blended from a 45% India large-cap / 20% India quality midcap / 20% global diversified ex-concentrated-AI / 15% EM-ex-India allocation, each leg's own rate researched — Nifty valuation, Shiller CAPE, EM consensus forecasts, etc., cited in the standalone sheet).

**A real bug caught during build**: the first `monthsToGoal` implementation searched forward *from* month 120 rather than from month 1, so any scenario that already blew past the goal before year 10 (the optimistic case, which reaches ₹20 Cr around year 7.8) silently reported "year 10.0" instead of the true, earlier crossing point. Caught by cross-checking the in-browser-equivalent Node computation against expectation before publishing, not by visual inspection — a reminder that a monotonic search still needs the right starting point.

**AI-boundaries note, for future sessions**: this screen computes numbers live in the render layer (`GOAL_PLAN` constants → `gpYearSeries`/`gpTotal`/etc.), which looks like it could clash with the house rule "no chart or card should compute a derived number in the render layer." It doesn't — that rule targets historical/real figures (XIRR, tax, valuations) that must trace to a real transaction ledger, not a disclosed, forward-looking what-if calculator where every assumption is a named constant the user can see and challenge. Don't quietly "fix" this into a static fixture; the live computation is the point of a planning tool.

**Not yet done**: the in-app screen doesn't (yet) let the user tweak the goal amount, SIP, or lumpsum inline — those are still hardcoded `GOAL_PLAN` constants, same as the standalone sheet. Making it an editable calculator is a natural next step if asked.

## Money tab v2 (2026-08-03, fifth build this day) — real line-item transactions, reclassify feature, honest layout fix

The user asked for three things after seeing Money v1: (1) scope to 2026 only, (2) real ideas for depth (typical-month baseline, category evolution, notable events, etc. — proposed first, per the "propose before building" memory), (3) fix the "boxes are random, too much blank space" layout complaint, and — the big one — "the misc, i want to classify them in the right buckets... i want to be able to look at the transactions with an option to classify them... check and implement." That last part meant checking whether the source PDFs actually have line-item transaction detail (they do) and, if so, extracting it for real.

**Data pull**: read all 6 statement PDFs in full (`Base resources/Statements to look and build/2026 credit card statements/`) — each has complete domestic + international transaction tables (date, merchant, amount, rewards, cardholder). Transcribed 514 real transactions into `mock-data/credit-card-transactions.js`. Deliberately excluded: CC PAYMENT rows (paying the bill, not spending), IGST-VPS correction reference lines (tax-adjustment noise tied to a parent transaction, not distinct purchases), PETRO SURCHARGE WAIVER credits, FINANCE CHARGES (already in credit-card-statements.js), 1% DCC adjustment lines, refund/reversal rows.

**The Europe trip (Slovenia/Croatia/Greece, ~25 days) was deliberately aggregated, not itemized line-by-line.** It generated ~130 individual international purchases averaging ~€15 each (gelato, parking, museum tickets). Itemizing every one would (a) bury the transaction list in noise no one needs to "reclassify," and (b) put an excessive amount of granular personal travel detail in a plaintext fixture for no real benefit. Instead: 5 real, summed aggregate entries (`agg: true` flag) — Slovenia/Croatia/Greece legs by real purchase count and total, plus flights/hotels booked abroad, plus international subscriptions. Pre/post-trip domestic spend and the flight/hotel bookings that were domestic-currency-billed (Yatra, GetYourGuide, Goibibo, Etraveli) stayed individually itemized — those are exactly the one-off, ambiguous entries a real classification feature is for.

**Two real double-counting bugs caught and fixed during this pass, before shipping**: (1) Beautiful Bizarre Magazine (₹3,732.88) had been included both as an individual itemized transaction AND inside the "international subscriptions" aggregate sum — removed from the aggregate. (2) The Yatra/GetYourGuide/Goibibo/Etraveli bookings (₹1,57,078 combined) were itemized individually as real domestic transactions AND also summed into a "flights & hotels" aggregate meant to be international-only — removed from that aggregate, which now correctly contains only the genuinely international-currency bookings (Aegean Airlines, Booking.com). Caught by writing a Python validation script that parsed the fixture and cross-checked totals, rather than trusting manual arithmetic across 500+ line items — **the lesson: for any dataset this size, transcribe once into a script and let code do the summing/validation, don't hand-verify arithmetic at this scale.**

**Reclassify feature, built as asked**: `categorizeTransaction(merchant)` — a deterministic, merchant-name-keyword rule set (same pattern as `fundCategoryOf()` for MF categories) — auto-assigns one of 13 categories to every transaction. Clicking any row in the new Transactions section (search + category filter) opens a `ReclassifyModal`; picking a category stores the override in `MoneyScreen`'s React state (in-memory only, resets on reload — no backend exists yet, disclosed in the UI copy) and the Category Mix chart above recomputes live from `realCategoryData(overrides)`. Verified in-browser: reclassifying "Apical Technologies (EMI)" from auto-assigned "Other" to "Shopping" immediately moved the category bars (Shopping 17.6%→19.2%, Other 20.1%→18.4%).

**"Typical month" replaces the flat average** (`typicalMonthData()`): median of the non-outlier months vs. the straight mean, with outlier months (>1.6× median, i.e. the real Europe-trip spike) called out by count rather than hidden inside a blended number — directly answers the "graphs don't show real value" and "average is misleading" complaints together.

**Layout fix — the "random boxes, too much white space" complaint**: `.insights-col` (shared by Insights and Money) changed from CSS grid (`repeat(auto-fit, minmax(...))`, which forces every card in a row to match the tallest one) to a **CSS multi-column masonry layout** (`columns: 2 440px`, `break-inside: avoid` per card, `column-span: all` for the `.span-2` chart sections). Cards now size to their own content and flow into whichever column has room — no more dead gaps. Saved as a standing feedback memory (`feedback_dashboard_layout.md`) since this pattern is reused across screens.

**Value labels added to `MonthlyChart`** (the "I don't even see value in the graphs" complaint) — every point now shows its real value directly on the chart, not just month labels on the x-axis.

**Verified in-browser 2026-08-03**: hard-reload required mid-session (ES module browser cache didn't pick up the edited `credit-card-statements.js` on a normal reload — documented as a recurring gotcha in this session). After that: "3 of 6 months" (not 7) confirms the Dec'25/Jan'26 statement is correctly excluded; Feb–Jul x-axis confirmed; reclassify flow confirmed end-to-end; Insights tab's masonry layout confirmed unaffected (shared CSS class). No console errors.

## Money tab (2026-08-03, fourth build this day) — real credit-card data, honest gaps everywhere else

Stage 4 in the roadmap, built next after the user asked to keep going one screen at a time. Grounded entirely in `mock-data/credit-card-statements.js` — 7 real HDFC Infinia statements (Jan–Jul 2026, both members share one card, Vinod primary/Keerthana add-on) that had been captured earlier in the session but never wired into any screen.

**What's real and shown**: two hero cards (latest statement's purchases + due date; 7-month average spend and utilisation) · a spend-trend line chart (real monthly purchases — May's spike is a real Europe trip the statement itself notes) · a credit-utilisation line chart (`available_credit_limit`/`credit_limit` per statement) · a "revolving balance check" reading the real `finance_charges` field (₹203 total across 3 of 7 months — trivial, called out as such, not alarmist) · a category-mix breakdown, **value-weighted** from each statement's disclosed top-5 categories (`purchases × category%`, summed across all 7 months — a real computation on real per-statement fields, not a fabricated aggregate) · a full statement-history table with a real Paid-in-full/Carried-balance badge per cycle (keyed off `finance_charges > 0`, not the sign of `total_amount_due` — a bug caught and fixed during build, since a positive due amount is just that cycle's normal bill, not evidence of carrying debt).

**What's honestly gap-noted, not fabricated**: the memo's Money tab spec (§14) wants Income/Expenses/Investments/savings-rate/runway and a full "closing loop" reconciliation — none of that is computable without a bank/savings statement, which doesn't exist in this app for either member. A prominent gap-note says so up top rather than inventing plausible-looking numbers. Category mix is also caveated as value-weighted from each statement's own top-5 disclosure, not true line-item spend (no transaction-level data on file — that's Module K / Stage 8+). The **Taxes** block exists but stays deliberately quiet (per the memo's N10 seasonal-promotion rule) — no real capital-gains computation exists yet to show even in-season.

**New shared chart**: `MonthlyChart` — a simple single-series line+dot SVG chart keyed by string month labels rather than the numeric-year x-axis `YearChart` uses; reused for both the spend trend and the utilisation trend.

**Verified in-browser 2026-08-03**: no console errors; a copy bug was caught and fixed mid-QA (the revolving-balance card said "well under ₹200" when the real total is ₹203 — replaced with a number-agnostic "a few hundred rupees" phrasing rather than a wrong hardcoded threshold).

## Insights tab (2026-08-03, third build this day) — Portfolio simplified, deep analysis moved to its own screen

Third pass the same day: the user asked to strip the insights rail back out of Portfolio ("remove the insights coloumn.. extend the table") and instead build the actual **Insights tab** (Stage 3, previously a placeholder) into the comprehensive, class-by-class analysis screen the whole day's work had been building toward — "detailed analysis for each of the assets class and also one together... a section for market analysis/news... use good graphs."

**Portfolio, simplified**: `PortfolioScreen` no longer renders `InsightsArea` at all. `.portfolio-body`'s two-column grid is gone — class-button row + member dropdown + `HoldingsTable` now run the full content width. The two hero cards stay (household + whatever's selected) since they're just numbers, not analysis. Row-click still narrows the scope hero card for a quick value/return check, but the pending-note now points at Insights ("open Insights" — an `.inline-link` that calls the same `onNavigate` used by Today→Portfolio) for anything deeper.

**`InsightsScreen`** (`index.html` §12, new): its own `classId`/`memberId` state (default `all`/`all` = whole-household overview) driving the same `CLASS_BUCKETS` button row, feeding `computeScope(classId, memberId, null)` — note `selectedId` is always `null` here; Insights operates at class granularity, not per-instrument (Portfolio's row-click already covers the per-asset case). A single `ScopeHeroCard` sits above the buttons. Below, the exact same `InsightsArea` (MF panel / general panel) that used to live in Portfolio's rail now renders full width.

**Layout became a real grid, not a narrow rail.** `.insights-col` changed from a `max-height:1400px; overflow-y:auto` flex column (a workaround for cramped rail space, and the source of some scroll-automation friction during QA) to `display:grid; grid-template-columns: repeat(auto-fit, minmax(420px,1fr))` — sections now flow 2–3 wide on a full page and the page just scrolls normally. Chart-bearing sections get a `.span-2` class to run full width.

**`GeneralInsightsPanel`** (replaces the old `StandardInsightsPanel`) is the one component handling every class except MF — it went from 2 possible sections (Trend, optional Sector) to a **data-driven stack of up to 9**, each rendering only when the current scope actually has the underlying real data — this is what makes it "specific per class" without hand-building 5 separate layouts:
- **Trend** (or, for the single EPF position, a **real growth + projection chart** — solid real EPFO history, dashed continuation at the account's own real CAGR, no fabricated growth rate).
- **Insights** — the existing `computeScopedInsights()` cards, unchanged.
- **Allocation + Across asset classes** — overview-only (`classId==='all'`): a real allocation donut (by L1) and a new `classComparisonData()` bar comparison of value + real return across all 6 classes, so the household "one view across everything" the user asked for is a first-class section, not just the household hero number.
- **Sector exposure** — wherever `scopePositions` include instruments with a real `sector` field (Indian/Foreign Equity, and the combined view in Overview).
- **Technical range scan** (`technicalScanData`, new) — every stock/RSU in scope plotted on its real 52-week range (0–100%), not just a single flagged name; genuinely more useful at class/household level than the old one-line technical insight.
- **Leaders & laggards** (`performanceRanking`, new) — real top/bottom performers by gain%, whenever ≥2 positions have cost-basis data.
- **Concentration scan** (`concentrationFlagsForClass`, new) — every position ≥8% of household net worth, not just the selected one.
- **Maturity ladder** (`maturityLadderData`, new) — real FD `rate`/`matures`/`value` as a sorted timeline; shows whenever any FD is in scope, including the household overview.
- **Market analysis & news** — `NewsSection`/`newsForPositions`, now used everywhere (previously MF-only), filtered to whatever's in scope; a real, always-present section per the user's explicit "section for market analysis/news" ask.

**`MFInsightsPanel`** kept its existing 5 sections (growth+projection, composition/overlap, cost & tax, rebalancing, news) and gained the same **Leaders & laggards** and **Concentration scan** sections as the general panel, for consistency.

**Bug found and fixed during this pass**: `scopeReturnMetrics()`'s EPF branch checked `scope.selected?.instrument_id === 'inst_epf_vinod'` — true only when a *row* had been explicitly clicked. The FD branch right next to it correctly checked the scope's resolved `scopePositions` instead. Since `InsightsScreen` never sets a `selectedId` (class buttons only), the EPF hero card was falling through to the generic "Total return vs. benchmark" branch instead of showing its real CAGR. Fixed both `scopeReturnMetrics` and `trendForScope`'s EPF checks to key off `scopePositions` (length 1, right instrument), matching the FD pattern and `GeneralInsightsPanel`'s own (correct) `isEpf` check.

**Known minor artifact, not fixed**: the Insights overview hero occasionally shows the real position-sum total (`scope.scopeTotal`, computed via `.reduce`) a rupee or two off from `householdTotals.current_total` (a separately-maintained constant used elsewhere, e.g. Today/Portfolio's household hero) — ordinary floating-point summation drift across 40 real position values, not fabricated data. Not worth correcting given the ₹1–2 magnitude against a ₹4.07 Cr total.

## Portfolio v4 (2026-08-03, same day) — dual hero cards + sectioned, class-aware insights

Second rebuild the same day, on top of v3, per a very detailed follow-up spec: drop the Institution column and compress the table; add a second hero card next to the household one, showing the *currently selected* class/member/asset's own value + return metrics; move the holdings table full-width below both hero cards; move the trend/insights area to a dedicated right column; and — the bulk of the ask — go deep on Mutual Fund insights specifically as a worked example (growth+projection chart, category/overlap composition, cost & tax, rule-based rebalancing, real news), then generalize the pattern to other classes.

**Layout**: `.portfolio-hero-row` (two cards, 1fr/1fr) sits above `.portfolio-body` (table col `1fr` + insights col `440px`). `PortfolioHeroCard` is unchanged content (household total + XIRR/TWR toggle). `ScopeHeroCard` shows whatever's currently selected (class/member/asset) — its own value, invested amount, and a return-metric pair from `scopeReturnMetrics()`.

**`scopeReturnMetrics()` — the "same representation, honestly filled" rule.** The user asked for "the same XIRR, TWR, overall gain%, invested amount" on the scope card. True per-scope XIRR/TWR isn't computable (no per-lot cash-flow dates — same limitation as the household figure). Rather than fabricate, this function picks the right *real* pair per scope:
- EPF asset selected → real CAGR (same one the trend chart already computes).
- An all-FD scope → the real, value-weighted **contracted rate** from each FD's `rate` field (FDs have a guaranteed rate — more honest than a market-style return here).
- Whole household, no filters → the existing household `xirr`/`twr` placeholders (disclosed-synthetic, unchanged).
- Everything else (a class, a member, a single stock/MF) → real unrealised **total return** ⇄ the relevant real benchmark's trailing-3-month return, with a caption explaining why this isn't literally XIRR/TWR. No scope ever shows a made-up number in an XIRR/TWR-shaped slot.

**Mutual Funds gets the deep treatment** (`MFInsightsPanel`, triggered whenever `classId==='mf'` OR a selected asset's class is `mf` — so it works for the whole MF sleeve *or* a single fund, same functions, just a narrower `scopePositions` list):
- **Growth & projection** (`YearChart`, a new hand-rolled multi-series year-axis SVG chart): invested + value curves are an *illustrative* year-by-year reconstruction, seeded and endpoint-anchored to the real total invested/current value (never a real per-year series — that needs CAMS transaction history, Stage 8, honestly captioned). A dashed continuation projects forward using Nifty 50's trailing 3-month return annualized (~6.6% p.a.) — a real-benchmark-derived, disclosed assumption, not an invented growth rate, and explicitly captioned "not a forecast or advice" per the AI boundaries (no advice, no ungrounded numbers).
- **Composition & overlap** (`mfCompositionData`): fund category (Index/ELSS/Smallcap/Midcap/Flexicap/Active) via a real name-based heuristic (`fundCategoryOf`), rendered as real % bars. Overlap detection is **ISIN-based, not guessed**: it caught that Keerthana's two Parag Parikh Flexi Cap folios share one ISIN (literally the same fund twice), plus a Flexicap-category duplication flag (PPFAS + quant). Sector/stock-level look-through is honestly gap-noted (no fund holdings-disclosure data on file) rather than fabricated.
- **Cost & tax**: real total unrealised gain, with an LTCG(12.5% above ₹1.25L exemption)/STCG(20%) explanation caveated for lacking per-lot dates. Expense ratio is an explicit gap note, **not a fabricated number** — flagged as a good next real-data pass (same AMFI/Yahoo-Finance pattern already used elsewhere), rather than guessing a plausible-looking TER.
- **Rebalancing** (`mfRebalancingBullets`): real, rule-based checks only — 3+ small/midcap funds, low index-fund %, and reusing the real overlap count. 1/3/5yr rolling returns and full sector-overweight are gap-noted (need historical fund NAV series, e.g. an mfapi.in-style API — roadmap, not built).
- **News & signals**: reuses the existing `newsDigest` fixture, filtered to instruments in scope, mapped red/amber/green by its real `impact` field — no new fabricated sentiment engine.

**Every other class/asset** (`StandardInsightsPanel`) keeps the v3 trend chart + `computeScopedInsights()` cards, now inside the same sectioned right column, plus a new **real sector-exposure bar section** (`sectorBreakdownData`) for Indian Equity / Foreign Equity / All — the one place besides direct-stock instruments where a real `sector` field exists, so this class of insight is only shown where it's actually backed by data.

**Verified in-browser 2026-08-03**: no console errors across the household/class/FD/MF hero-card variants, the full MF section stack (growth chart → composition → cost & tax → rebalancing → news), and single-fund selection (Axis ELSS) correctly narrowing every section to that one holding.

## Portfolio v3 (2026-08-03) — class-button dashboard with scope-aware analysis

Full rebuild per the user's detailed spec ("first in all pages move the sidebar to the top... categorise the table with asset class MF, indian equity, foreign equity, epf, fixed income as buttons... bottom needs to be a trend for the selection... insights... think deep and implement, this cannot be normal"). Replaces Portfolio v2 (donut + 6-lens dropdown + member chips + search + grouped table + 8 static insights).

**Layout**: `.portfolio-top` is a 280px/1fr grid. Left (sticky): the big hero value + XIRR⇄TWR toggle, unchanged from v2 ("perfect" per the user). Right: a class-button row (`CLASS_BUCKETS` in `index.html` §11 — All/Mutual Funds/Indian Equity/Foreign Equity/EPF/Fixed Income/Real Assets, each showing a live count) plus a Member `<select>`, then the holdings table. **Default class: Foreign Equity** (per spec). `Real Assets` (Gold ETF) was added beyond the 5 the user named, since Gold didn't fit any of the 5 and needed a home. Below both columns, full-width: `.analysis-row`, a trend chart + insights column that responds to whatever is currently selected.

**Selection model**: `classId` (button) × `memberId` (dropdown) filter the table. Clicking a row additionally sets `selectedId`, scoping the trend+insights row to that one asset; clicking the same row again clears it. Changing class/member clears any row selection. When nothing is row-selected, the trend+insights scope is the current class/member filter (or the whole household if both are "All") — this satisfies "if none selected then for the entire portfolio."

**Trend chart** (`TrendChart` component, hand-rolled SVG, no charting lib): dual-line, **both series rebased to start=100** and plotted by fractional index position rather than date, specifically so a 90-point illustrative series and a 30-point real benchmark series (different lengths, different timescales) can share one 0–1 x-axis without pretending to be date-aligned. Two data sources, deliberately not blurred together:
- **EPF, when the individual EPF asset row is selected**: the ONE real, decade-scale series in the app (`epf-history.js`, real EPFO yearly balances 2018–2026). Shown alone, no benchmark overlay — a 9-year EPF series against a 3-month equity index would be a misleading comparison, so the benchmark is intentionally omitted for EPF and replaced with a real CAGR figure instead of the usual "aggregate unrealised return."
- **Everything else**: `buildIllustrativeTrend()` — a seeded random walk from the scope's implied cost basis to its current value, clearly labeled "Illustrative" in the card. Overlaid with a REAL 3-month benchmark line from `market-data.js` (`BENCHMARKS.nifty50` for INR-majority scopes, `BENCHMARKS.nasdaq` for USD-majority scopes) — real data, but explicitly captioned "not a precise same-period comparison" since the illustrative side isn't date-matched to it.

**"XIRR/TWR across assets, classes, benchmark, own growth"**: household-level XIRR/TWR (still synthetic, per the known-gaps note above) stayed in the hero exactly as before. Below that, every scope (class/member/asset) gets its own **real, computed** value-weighted "aggregate unrealised return" (`scope.aggReturn` in `computeScope()`) plus the real benchmark's trailing-3-month return — this is the honest stand-in for a true per-scope XIRR, which isn't computable yet without per-lot cashflow dates (same limitation as the household figure).

**Insight engine** (`computeScopedInsights()`): two branches.
- **Asset selected**: `technicalInsight()` (52-week range position + day range + volume, only for instruments carrying a `tech` object — i.e. stocks/RSU, not MF/FD/EPF/Gold, which correctly get no fabricated technical read), `peerComparisonInsight()` (real gain% vs. the average of same-class peers, flags >12pt underperformance or >25pt outperformance), `concentrationInsight()` (flags >8% of household net worth), plus any `SPECIAL_BY_INSTRUMENT` entry for that specific holding (reuses the 8 original portfolio-wide insights — e.g. selecting the RSU surfaces the employer-concentration insight, selecting either Sensex fund surfaces the direct-vs-regular cost-drag insight).
- **Class/member/portfolio scope**: `CLASS_RELEVANT[classId]` picks which of the 8 original insights actually apply to that asset class (e.g. Fixed Income gets the NBFC-credit-risk + liquidity insights, not the semiconductor-theme one), plus two dynamic ones computed fresh from whatever's currently filtered: `sectorConcentrationInsight()` (real sector data from `instruments.js`, flags >28% concentration in one sector) and a peer-underperformer flag computed from the filtered set's own gain% spread.

All of this reads real fields already in `positions.js`/`instruments.js`/`market-data.js`/`epf-history.js` at render time — nothing in the insight engine is a hardcoded string, consistent with the P8/P1 discipline (AI boundaries section above) of never generating a UI number with no code path behind it.

**Verified in-browser 2026-08-03**: no console errors across Today, Portfolio "All" grouped view, every class button, member dropdown, and row-selection (tested NVDA — technical/performance/concentration/theme insights all populated correctly; tested EPF — real CAGR trend with no benchmark, as designed).

## Display convention: NAV and quantity

Every position in `positions.js` now carries `units` (quantity held) and `nav` (price per unit, in the instrument's native currency) wherever known, alongside `current_value`. **Any UI that lists a holding should show units and NAV, not just the total value** — this is what let the AMFI mismatch above actually get caught; a bare total value can't be sanity-checked by the user, `units × nav` can. Keep this going forward for any new holdings screen.

**Resolved:** Vinod's FDs showed a real discrepancy — the screenshot's 3 named FDs (Shriram Finance + 2 HDFC Bank, ₹25L invested / ₹29.54L current) vs. INDmoney's own live aggregate (₹15L invested / ₹17.46L current, unnamed single bucket, likely missing the NBFC deposit). Asked the user directly 2026-08-02; they confirmed **trust the screenshot's 3 named FDs** — that's what's in `positions.js`. Don't re-raise this unless new FD data shows up.

## Build status

| Stage | Scope | Status |
|---|---|---|
| 0 | Repo scaffold, design tokens, shell, mock fixtures | done — **rebuilt 2026-08-03**: top nav shell replaces sidebar (see decision table above) |
| 1 | TODAY tab (landing) | done (v2, desktop) — two-column (`today-grid`), hero+What Changed+mini-allocation-donut left, agenda+Needs You right, real household data, verified in-browser, no console errors |
| 2 | PORTFOLIO tab | done (**v5, 2026-08-03**) — see "Insights tab" section above for the full design: dual hero cards (household + selected scope, honest per-scope return metrics), class-button filter (default Foreign Equity) + Member dropdown + a full-width compressed sortable table (no Institution column). The insights rail was removed from this screen and moved to its own tab. **Not yet built**: full sunburst/treemap chart (Donut is still hand-rolled SVG, not ECharts), a dedicated Position-detail screen (row selection + the Insights tab cover much of this already), remaining lenses from memo §7 (geography/theme/conviction), Glance⇄Study toggle |
| 3 | INSIGHTS tab | done (**2026-08-03**) — see "Insights tab" section above for the full design: class-button + Member filter drive a full-width, data-driven grid — trend/real-growth, insight cards, household allocation + cross-class comparison (overview only), sector exposure, technical range scan, leaders & laggards, concentration scan, FD maturity ladder, and market news, each rendering only where the underlying data actually supports it. Mutual Funds gets its own deeper 7-section panel (growth+projection, composition/overlap, cost & tax, rebalancing, leaders/laggards, concentration, news). **Not yet built**: flag-decision/assumptions sub-screens (memo §10/§11.2), 1/3/5yr rolling returns and expense ratios (both honestly gap-noted pending a real data source), true fund look-through for sector/stock exposure. |
| 4 | MONEY tab | done (**2026-08-03**) — see "Money tab" section below for the full design |
| 4a | GOAL tab (₹20 Cr growth plan) | done (**2026-09-02**) — not in the original memo's stage numbering; added by explicit user request. See "Goal tab" section above. 10-year scenario calculator (optimistic/base/pessimistic), investable/liquid wealth only — immovable property deliberately excluded, not a bucket in this model. **Not yet built**: inline editing of the goal/SIP/lumpsum constants (currently hardcoded `GOAL_PLAN`, though FD/EPF/Gold within it are live-derived from `positions`). |
| 4b | BUDGET tab (real income/outflow) | done (**2026-09-02**) — not in the original memo's stage numbering. See "Budget tab" section above. One real month, **46.25%** real savings rate (MF SIP only), cross-linked with the Goal tab's required-SIP gap, a real liquidity-runway figure, and a live budget-health-insights section. **Not yet built**: multi-month trend (only one data point exists). |
| 4c | DEBT & IMMOVABLE ASSETS tab (loans, real estate, debt-vs-invest) | done (**2026-09-02**) — not in the original memo's stage numbering. See "Debt tab" section above. 3 real masked loan accounts against 2 real properties (`mock-data/real-estate.js`, kept fully outside `positions`/net worth by design); per-loan rate vs. Goal-plan new-SIP-scenario comparison, not a recommendation. **Not yet built**: syncing real estate/debt into the standalone `investment-plan-20cr.html` sheet (which never got real estate added, so this was never a divergence to fix). |
| 5 | ADD sheet / review queue UI | not started |
| 6 | Settings screen | not started — sidebar nav link exists (`Sidebar` component), no screen behind it |
| 7 | Mobile layout pass | **deferred, not started** — was "wide/tablet pass" when mobile was the default; now inverted. Desktop is the default build target (see decision table). Revisit only if the user asks for mobile back. |
| 8 | Real Firebase backend + ingestion pipeline | not started |

## Open decisions (memo §19 — deferred, not blocking current UI stages)

- Firestore region (`asia-south1`? — check before creating any real Firebase project; immutable after creation)
- Base currency and single- vs multi-jurisdiction tax model
- History reconstruction epoch (how many years of statements to backfill)
- Illiquid asset valuation policy (frequency, source, caveat prominence)
- Advice boundary (categories + trade-offs only, never specific products — already a hard rule, not actually open)
- Ledger store timing (Firestore vs relational-shaped-in-Firestore vs Cloud SQL later) — decide at Stage 8/Phase 4, not now

None of these block Stages 0–7. Don't resolve them prematurely; don't forget they exist.

## Session conventions (easy to accidentally violate)

- No comments unless the WHY is non-obvious. No multi-line doc blocks.
- No client-side money math (S8) — even in mock stages, fixtures carry pre-computed values; the render layer never derives XIRR/returns/tax from raw transactions.
- **Desktop-first now, not mobile-first** (superseded 2026-08-02 — see decision table). Design for `--content-max-w: 1560px` (widened 2026-08-03 when the sidebar became a top bar) with multi-column layouts. Mobile (390px) is Stage 7, deferred, not the default target — don't reflexively build for a 480px frame anymore.
- **Nav lives in a top bar, not a left sidebar** (superseded 2026-08-03 — see decision table). Don't reintroduce a `Sidebar`/left rail without the user asking; the whole point was reclaiming horizontal space.
- `Glance ⇄ Study` toggle is per-member, not per-household (memo §14B.5) — not built yet, still applies whenever it is.
- Today: max 3 "what changed" cards (N4) — this survived the desktop rewrite; it's an attention-budget rule, not a screen-size one. Insights: max 5 active flags (N4, §11.3, not yet built).
- Max 4 charts per screen (N5); one message per chart (N6). Portfolio's insight cards are text+numbers, not charts — doesn't count against this.
- Nav was frozen at four sections (Today/Portfolio/Insights/Money) per N7 — **the user explicitly asked to add a 5th, "Goal", on 2026-09-02**, so N7 is no longer a hard cap; don't reflexively add tabs without the same kind of explicit ask. **Money was then hidden from the nav the same day** (also explicit). **"Budget" and then "Debt" were added right after** (also explicit — genuinely new tabs, not un-hiding Money), and **"Debt" was renamed "Debt & Immovable Assets"** once real estate moved into it. **The user then specified an exact tab order**, also 2026-09-02: visible order is now **Today / Portfolio / Debt & Immovable Assets / Budget / Insights / Goal** (7 items on `NAV_ITEMS` counting the hidden Money row, which stays wherever it was in the array since it doesn't render). This is not alphabetical or build-order — it's a deliberate order the user chose; don't silently re-sort `NAV_ITEMS` for any reason. `MoneyScreen`, its route, and its data are all still in `index.html` untouched; only its `NAV_ITEMS` row was removed. Don't re-add Money without being asked, and don't treat this nav growth as license to keep adding tabs freely — every addition so far has been a specific, explicit ask, not a judgment call made unprompted.
- Undo, not confirm (U5) — no "Are you sure?" dialogs except delete-account/export.
- Indian numbering by locale (₹3.93 Cr, not ₹39,300,000) — U9.
